# 📝 trip.lalalakorea.com 새 글 작성 가이드

---

## ✅ 자동으로 적용되는 기능들 (따로 할 것 없음)

새 글을 `.md` 파일로 만들기만 해도 아래가 **자동** 적용됩니다:

| 기능 | 적용 방식 |
|------|----------|
| 목차(TOC) | `##`, `###` 제목이 2개 이상이면 자동 생성 |
| 소셜 공유 버튼 | 링크복사, 카카오, X, 페이스북, 네이버 자동 표시 |
| 관련 글 추천 | 같은 카테고리 최신 글 4개 자동 표시 |
| OG 태그 / SNS 미리보기 | frontmatter 정보 자동 적용 |
| JSON-LD 구조화 데이터 | 구글 검색 노출용 자동 생성 |
| 페이지네이션 | 목록 자동 분류 (20개씩) |

---

## 🗂️ 새 글 파일 만드는 법

**파일 위치:** `src/content/blog/[숫자].md`
→ 현재 133번까지 있으므로 `134.md`, `135.md` 순서로 만들면 됩니다.

### 필수 frontmatter 템플릿

```markdown
---
entry_slug: "글제목을-영어나-한국어로-하이픈으로-연결"
title: "글 제목 (검색 키워드 포함해서 쓰기)"
date: 2026-03-28
category: "해외 여행"
description: "이 글의 핵심 내용을 150자 이내로 요약. 구글 검색 결과에 표시됩니다."
---

본문 내용...
```

### 카테고리 목록 (기존 카테고리 그대로 쓸 것)
- `해외 여행`
- `국내 여행`
- `지구 상식`
- `지구 위기& 재난 대비`
- `여행`
- `캠핑`
- `커피`

### entry_slug 규칙
- 제목을 소문자 하이픈으로: `파나마-운하-위치-역사-정보`
- 특수문자, 괄호, 공백 없이
- 너무 길면 핵심 키워드만: `일본-vs-한국-비교-면적-인구`

---

## 🤖 AI(Claude)에게 글 써달라고 할 때 사용하는 프롬프트

아래 프롬프트를 복사해서 Claude에게 붙여넣고, `[주제]` 부분만 바꾸면 됩니다.

---

### 📋 복사용 프롬프트

```
다음 조건에 맞춰 여행 블로그 글을 마크다운으로 작성해줘.

주제: [여기에 주제 입력 - 예: 포르투갈 여행 정보, 아이슬란드 vs 한국 비교]

[조건]
1. frontmatter 형식 지켜서 작성:
---
entry_slug: "핵심키워드-하이픈연결"
title: "검색에 잘 걸리는 제목 (핵심 키워드 포함)"
date: 오늘날짜(YYYY-MM-DD)
category: "해외 여행"  ← 주제에 맞게 변경
description: "글 핵심 내용 요약 120~150자. 구글 검색 결과에 표시됨."
---

2. 본문 구조:
- 도입 단락: 이 글을 쓰게 된 계기나 흥미로운 사실로 시작
- ## 제목으로 3~6개 섹션 구성 (TOC 자동 생성됨)
- ### 소제목으로 세부 내용 구분
- 마지막에 짧은 마무리 단락

3. 글 스타일:
- 딱딱하지 않고 친근한 한국어 구어체
- "~입니다" 보다 "~이다", "~한다" 자연스러운 문체
- 숫자, 통계, 비교 데이터 포함 (있으면)
- 너무 길지 않게 800~1500자 분량

4. SEO 최적화:
- 제목에 검색 키워드 포함 (예: "~위치", "~정보", "vs 한국 비교")
- description은 독자가 클릭하고 싶게 작성
- 소제목(##)에도 키워드 자연스럽게 포함

마크다운 코드블록으로 감싸서 바로 복사할 수 있게 줘.
```

---

## 🚀 새 글 발행 체크리스트

글을 다 썼으면 아래 순서대로 하면 됩니다.

### Step 1. 파일 생성
`src/content/blog/134.md` 파일 만들고 내용 붙여넣기

### Step 2. 스크립트 2개 실행 (PowerShell에서)
```powershell
cd 클로드cowork\trip-blog
python insert_internal_links.py   # 내부 링크 자동 추가
```
> 💡 description은 이미 frontmatter에 직접 쓰면 되니까 add_meta_descriptions.py는 안 돌려도 됩니다.

### Step 3. Git Push → 자동 배포
```powershell
git add .
git commit -m "새 글: [글 제목]"
git push
```

→ Vercel이 자동으로 감지해서 1~2분 내 배포 완료 ✅

---

## 📸 이미지 추가할 때

이미지는 `public/images/[파일번호]/` 폴더에 저장하고 마크다운에서:
```markdown
![설명](/images/134/이미지파일명.jpg)
```

thumbnail 지정하고 싶으면 frontmatter에:
```markdown
thumbnail: "/images/134/대표이미지.jpg"
```

---

## 💡 자주 쓰는 마크다운 패턴

```markdown
## 섹션 제목 (TOC에 나옴)

### 소제목 (TOC에서 들여쓰기로 나옴)

**굵게** 표시

> 인용 블록 (배경색 표시)

| 항목 | 내용 |
|------|------|
| 수도 | 서울 |

- 리스트 항목
- 리스트 항목
```
